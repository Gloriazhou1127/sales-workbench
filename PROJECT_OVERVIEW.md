# 销售工作台 — 项目概览

> 最后更新：2026-06-17 | 作者：李柏言

---

## 一、项目定位

**销售工作台**是一套面向企业事业部销售团队的内部管理系统，解决大客户（KA）跟进的数字化管理问题。核心场景：**盯住本财年新增的大客户（教室数 ≥ 30），追踪从初次接触到最终成交的全生命周期。**

当前聚焦「硬件打点跟进」模块，后续可扩展软件续费、新品增购等模块。

---

## 二、设计思路

### 2.1 架构选型：简单优先

| 决策 | 选择 | 原因 |
|------|------|------|
| 后端框架 | Flask | 轻量，215 人规模不需要 Django |
| 数据库 | SQLite | 零配置，单文件易备份，不必装 MySQL |
| 前端 | 原生 HTML/CSS/JS | 无框架构建步骤，修改即刷新 |
| 图表 | Chart.js（CDN） | 开箱即用，不增加依赖 |
| 鉴权 | JWT | 无状态，适合后续分布式部署 |
| 部署 | Nginx + Gunicorn | 生产级压缩、HTTPS、多进程 |

**原则：能用简单方案就不上重型武器。** 项目目前只有 8 个页面、20+ 个 API，4,800 行代码，一个人维护，无需微服务或前后端分离。

### 2.2 数据流

```
CRM（销售易）→ 批量拉取 → SQLite ← 用户手动编辑
                    ↓
              API 权限过滤
                    ↓
              前端展示 + 图表
```

核心设计：**CRM 数据自动同步 + 销售手动跟进**，两者在 `key_account_hardware` 表合并。

### 2.3 权限模型

从花名册 Excel 导入 215 人组织架构，支持四级层级：

```
管理员         → 看全部（194 条）
三级组织负责人 → 看自己 + L3 下属
四级组织负责人 → 看自己 + L4 下属
城市管理       → 同四级组织负责人
普通销售       → 仅看自己名下客户
```

权限在 SQL 层用 `LIKE '姓名-%'` 前缀匹配实现，后端 API 统一调用 `get_owner_filter()`。

---

## 三、功能模块

```
销售工作台
├── 登录页              ← 角色筛选 + 用户名搜索 + 记住密码
├── 工作台首页           ← 8 模块卡片导航
├── 管理后台
│   ├── 模块链接配置      ← 管理员编辑首页卡片
│   └── 权限管理         ← 用户账号 / 组织架构 / 权限规则
└── 大客户跟进
    ├── 硬件打点跟进（填报明细）
    │   ├── 统计卡片（总数/已打点/阶段分布）
    │   ├── 筛选栏（阶段/状态/部门/搜索）
    │   ├── 跟进表格 + 侧滑编辑面板
    │   ├── 导出 CSV
    │   └── 数据导入 Tab（仅 admin）
    ├── 数据分析
    │   ├── KPI 汇总卡片（6 项指标）
    │   ├── 每月新增客户趋势（柱状图 + 汇总表）
    │   ├── 每月打点客户趋势
    │   ├── 阶段分布（柱状图 + 汇总表 + 下钻）
    │   ├── 阶段 6 无采购意向明细
    │   └── 转化漏斗（借用 → 采购）
    ├── 软件续费             ← 占位，即将上线
    └── 新品增购             ← 占位，即将上线
```

---

## 四、工作任务与进度

### 4.1 总体统计

| 指标 | 数值 |
|------|------|
| 总任务数 | **48 项** |
| 已完成 | **45 项（93.8%）** |
| 进行中 | 1 项 |
| 待处理 | 2 项 |
| 代码行数 | ~4,800 行（Python 1,561 + HTML 3,253） |
| 数据库表 | 7 张，总行数 800+ |
| 用户量 | 215 人（从花名册导入） |
| 客户数据 | 194 条硬件客户 |

### 4.2 按阶段划分

#### 阶段一：基础设施（任务 #1-#6）✅ 全部完成

搭建 Flask 后端骨架、JWT 鉴权、SQLite 数据库、8 页面 HTML 结构。管理后台支持模块链接配置，工作台首页可动态渲染。

#### 阶段二：核心业务（任务 #7-#11）✅ 全部完成

硬件打点跟进全流程打通——从 CRM 批量拉取 193 条客户数据入库、拉取订单/商机/拜访辅助数据、前端统计卡片 + 筛选 + 表格 + 侧滑编辑 + CSV 导出。自动打点规则（借用或采购 > 0 → 已打点阶段 1）。

#### 阶段三：数据分析（任务 #20-#31）✅ 全部完成

Chart.js 图表系统——KPI 卡片、月趋势柱状图、阶段分布、下钻弹窗、转化漏斗。支持部门筛选、数据聚合、汇总表格联动。两页面 Tab 导航统一。

#### 阶段四：权限系统（任务 #32-#41）✅ 全部完成

花名册 215 人导入、`org_hierarchy` 表 372 条层级关系、`get_owner_filter()` 统一 SQL 过滤、所有 8 个分析 API 接入权限。权限管理页面支持用户编辑和花名册重导。Bug 修复：LIKE 前缀匹配（`owner_name` = "姓名-工号" vs `display_name` = "姓名"）。

#### 阶段五：用户体验（任务 #42）✅ 全部完成

登录页改造——角色下拉（192 销售 / 18 城市管理 / 4 管理员）、用户名 `<datalist>` 搜索、记住密码。删除过时的「同步 CRM 数据」按钮，改为数据导入模块。

#### 阶段六：生产就绪（任务 #43-#48）✅ 全部完成

全配置环境变量化、随机 SECRET_KEY、生产模式 CORS 限制、`requirements.txt`、Gunicorn 多 worker + systemd 开机自启、Nginx HTTPS 反向代理、SSL 自动续签、每日备份脚本。修复 `datetime.utcnow()` 弃用。

#### 待处理（2 项）

| 任务 | 说明 |
|------|------|
| #8 并行拉取订单/商机/拜访数据并聚合入库 | 借用/采购/拜访/商机数据目前部分为 0，需通过 CRM API 批量补充 |
| #14 更新导入脚本备用 | 本地 Excel/CSV 导入脚本需适配新表结构 |

> 这两项属于数据补充类，不影响系统正常使用。

### 4.3 完整任务清单

```
✅ #1   构建销售工作台系统
✅ #2   搭建 Flask 后端（含模块管理 API）
✅ #3   创建管理后台页面
✅ #4   创建登录页面
✅ #5   创建工作台主页面
✅ #6   开发大客户跟进模块骨架
✅ #7   开发硬件打点跟进核心功能
⏳ #8   并行拉取订单/商机/拜访数据并聚合入库
✅ #9   拉取订单数据（前 20 页）
✅ #10  拉取商机数据（前 5 页）
✅ #11  拉取拜访记录（前 5 页）
✅ #12  报告结果
✅ #13  撰写并执行 Python 聚合脚本
⏳ #14  更新导入脚本备用
✅ #15  更新前端显示完整数据
✅ #16  导入本地 CSV/Excel 数据到 SQLite
✅ #17  自动打点逻辑 + 管理员阶段配置 + 商机金额/状态
✅ #18  筛选栏增加所属部门下拉
✅ #19  后端 API 支持分析数据
✅ #20  创建数据分析页面
✅ #21  扩展阶段字段（阶段 4/6）
✅ #22  检查项目当前状态
✅ #23  启动服务器并验证功能
✅ #24  修复发现的问题
✅ #25  修复 stats API 的 SQL 注入漏洞
✅ #26  修复 created_date 格式不一致
✅ #27  端到端验证前端页面功能
✅ #28  添加缺失的 sync-crm-accounts 接口
✅ #29  修正价值复盘完成率计算
✅ #30  在填报明细页添加 Tab 切换导航
✅ #31  调整数据分析看板布局和逻辑
✅ #32  改造 users 表并导入花名册
✅ #33  为大客户跟进 API 添加权限过滤
✅ #34  创建数据导入页面（第三个 Tab）
✅ #35  创建权限管理模块页面
✅ #36  更新所有页面 Tab 导航和路由
✅ #37  验证数据导入页面功能
✅ #38  验证导入花名册 API
✅ #39  验证非 admin 账号权限过滤
✅ #40  验证权限管理页面
✅ #41  验证数据导入 Tab 可见性
✅ #42  登录页增加角色选择、用户名下拉、记住密码
✅ #43  评估项目线上化方案
✅ #44  创建部署配置文件
✅ #45  改造 app.py 配置为环境变量模式
✅ #46  创建生产环境启动配置
✅ #47  修复安全问题和弃用代码
✅ #48  创建云服务器部署配置包
```

---

## 五、技术栈总览

```
前端：HTML5 + CSS3 + Vanilla JS + Chart.js v4.4.6
后端：Python 3.13 + Flask + JWT (PyJWT)
数据库：SQLite 3（workbench.db, 184 KB）
认证：JWT（24 小时过期，localStorage 存储）
部署：Nginx + Gunicorn（8 worker）+ systemd
安全：werkzeug 密码 Hash、参数化 SQL、HTTPS + Let's Encrypt
```

---

## 六、当前状态

| 维度 | 状态 |
|------|------|
| 功能完整度 | **核心闭环已打通** — 登录→打点→分析→导出，全链路可用 |
| 数据完整度 | 194 条客户 + 215 用户 + 6 阶段配置，借用/采购/商机部分缺数据 |
| 权限完备度 | 四级权限模型全 API 覆盖，L3/L4 层级过滤正确 |
| 生产就绪度 | 环境变量化 100%，一键部署脚本就绪，待购买服务器执行 |
| 已知缺陷 | 数据导入支持 customer 类型，其他类型（order/visit 等）为占位 |

### 项目可对外展示的截图页面

| 页面 | 亮点 |
|------|------|
| 登录页 | 角色筛选 + 搜索用户名 + 记住密码 |
| 工作台首页 | 8 模块卡片，动态渲染 |
| 硬件打点跟进 | 194 条客户、统计卡片、阶段编辑、CSV 导出 |
| 数据分析 | 6 个 KPI、3 张图表、下钻弹窗、转化漏斗 |
| 权限管理 | 215 用户表格、组织架构查看、花名册一键重导 |
| 管理后台 | 模块链接配置、数据导入 |

---

## 七、下一步计划

1. **部署上线**（待用户提供腾讯云服务器信息后一键部署）
2. **补充 CRM 数据**（通过 API 补齐借用/采购/拜访/商机数据）
3. **软件续费模块**（按现有骨架扩展第二个子模块）
4. **数据导入完善**（支持 Excel/CSV 多类型批量导入）
